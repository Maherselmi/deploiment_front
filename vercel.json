{
  "buildCommand": "npx ng build",
  "installCommand": "npm install --legacy-peer-deps",
  "outputDirectory": "dist/insureflow/browser",
  "framework": "angular",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
